# HUD Placeholder

This directory will contain the HUD / UI components for Jarvis. Add React/Vue/Svelte components here.

Suggested structure:
- `src/` — UI components
- `public/` — static assets
- `electron/` — desktop integration (optional)

Start by creating a small web socket client to receive events from `core` and render them in the HUD.
