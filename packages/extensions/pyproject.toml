[project]
name = "jarvis-extensions"
version = "0.1.0"
description = "JARVIS package: extensions"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling>=1.25.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/jarvis_extensions"]
