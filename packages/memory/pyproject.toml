[project]
name = "jarvis-memory"
version = "0.1.0"
description = "JARVIS package: memory"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling>=1.25.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/jarvis_memory"]
