[project]
name = "jarvis-webintel"
version = "0.1.0"
description = "JARVIS package: webintel"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling>=1.25.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/jarvis_webintel"]
